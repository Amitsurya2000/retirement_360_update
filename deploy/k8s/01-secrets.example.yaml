# Copy to 01-secrets.yaml and fill in real values. DO NOT commit the filled version.
#   kubectl apply -f 01-secrets.yaml
apiVersion: v1
kind: Secret
metadata:
  name: retire360-secrets
  namespace: retire360
type: Opaque
stringData:
  POSTGRES_PASSWORD: "change-me"
  DATABASE_URL: "postgresql+psycopg://retire:change-me@postgres:5432/retire360"
  GEMINI_API_KEY: "AIza..."
